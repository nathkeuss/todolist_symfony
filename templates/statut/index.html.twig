{% extends 'base.html.twig' %}

{% block title %}Hello StatutController!{% endblock %}

{% block body %}

<div class="example-wrapper">

    <a href="{{ path('create_statut') }}">Créer un statut</a>

    {% for statut in status %}
        <p>{{ statut.name }}</p>
    {% endfor %}

</div>
{% endblock %}
